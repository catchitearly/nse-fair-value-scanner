# curl_cffi: impersonates Chrome TLS fingerprint — bypasses Yahoo Finance
# bot-detection that blocks GitHub Actions IP ranges.
# yfinance uses it automatically when installed (via yf.Ticker session).
curl_cffi>=0.7.0
yfinance>=0.2.54
pandas==2.2.3
numpy==1.26.4
requests==2.32.3
beautifulsoup4==4.12.3
tqdm==4.66.4
openpyxl==3.1.5
tabulate==0.9.0
python-dotenv==1.0.1
lxml==5.3.0
